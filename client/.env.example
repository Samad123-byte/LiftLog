# Same Vercel project: leave this unset because the client uses /api automatically.
# Separate backend project: set the full backend API URL instead.
# VITE_API_URL=https://your-backend-project.vercel.app/api
